<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_agent_tree" model="ir.ui.view">
        <field name="name">odoo.agent.tree</field>
        <field name="model">odoo.agent</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="status" widget="badge"/>
                <field name="runtime_id"/>
                <field name="engine"/>
                <field name="model"/>
                <field name="active_execution_count"/>
                <field name="max_concurrent_executions"/>
            </list>
        </field>
    </record>

    <record id="view_agent_kanban" model="ir.ui.view">
        <field name="name">odoo.agent.kanban</field>
        <field name="model">odoo.agent</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <field name="status"/>
                <field name="runtime_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title"><field name="name"/></strong>
                                <div><field name="status" widget="badge"/></div>
                                <div>Runtime: <field name="runtime_id"/></div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_agent_form" model="ir.ui.view">
        <field name="name">odoo.agent.form</field>
        <field name="model">odoo.agent</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="status" widget="badge"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_executions" type="object" class="oe_stat_button" icon="fa-play-circle">
                            <field name="active_execution_count" widget="statinfo" string="Active Executions"/>
                        </button>
                    </div>
                    <div class="alert alert-info" role="alert">
                        <strong>Agent setup tip:</strong>
                        Choose the runtime machine, then choose the engine/CLI that runtime can execute.
                        Use Custom CLI when the runtime already knows how to call a proprietary agent.
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="mention_key" placeholder="qa-agent"/>
                            <field name="description"/>
                            <field name="runtime_id"/>
                            <field name="engine"/>
                            <field name="cli_command"/>
                            <field name="model"/>
                        </group>
                        <group>
                            <field name="max_concurrent_executions"/>
                            <field name="timeout_seconds"/>
                            <field name="retry_limit"/>
                            <field name="user_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="instructions" string="Instructions">
                            <div class="alert alert-light" role="alert">
                                Write the agent's operating rules here: role, boundaries, project context,
                                expected output format, and what it must never do without human approval.
                                Other agents can mention this agent with its @handle.
                            </div>
                            <field name="instructions" nolabel="1"/>
                        </page>
                        <page name="skills" string="Skills">
                            <div class="alert alert-light" role="alert">
                                Skills are reusable capability packs injected into the final prompt.
                                Add only the skills this agent needs; too many skills make executions noisy and harder to audit.
                            </div>
                            <field name="skill_ids" widget="many2many_tags"/>
                        </page>
                        <page name="mcp" string="MCP Servers">
                            <div class="alert alert-light" role="alert">
                                MCP servers expose tools and data sources to the agent runtime.
                                Assign only trusted servers and verify their commands, URLs, and environment variables on the runtime host.
                            </div>
                            <field name="mcp_server_ids" widget="many2many_tags"/>
                        </page>
                        <page name="executions" string="Executions">
                            <div class="alert alert-light" role="alert">
                                Each execution is one auditable run against a Project task: queued, running,
                                waiting for input, completed, failed, or cancelled.
                            </div>
                            <field name="execution_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="status" widget="badge"/>
                                    <field name="runtime_id"/>
                                    <field name="task_id"/>
                                    <field name="create_date"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_agent_search" model="ir.ui.view">
        <field name="name">odoo.agent.search</field>
        <field name="model">odoo.agent</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="runtime_id"/>
                <field name="engine"/>
                <filter name="idle" string="Idle" domain="[('status', '=', 'idle')]"/>
                <filter name="working" string="Working" domain="[('status', '=', 'working')]"/>
                <group expand="0" string="Group By">
                    <filter name="group_status" string="Status" context="{'group_by': 'status'}"/>
                    <filter name="group_runtime" string="Runtime" context="{'group_by': 'runtime_id'}"/>
                    <filter name="group_engine" string="Engine" context="{'group_by': 'engine'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
